You are given a string S.
Your task is to print all possible size k replacement combinations of the string in lexicographic sorted

from itertools import combinations_with_replacement
 
s, n = input().split(' ')
n = int(n)
s = sorted(s)

for x in sorted(set(combinations_with_replacement(s,n))):
    print(*x, sep='')
